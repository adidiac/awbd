server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: note-service
          uri: lb://note-service
          predicates:
            - Path=/notes/**
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
